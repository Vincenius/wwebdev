// Shared data for the RSS / sitemap generators. Same source and order as the
// site itself (src/lib/content.ts): posts + templates, newest first.
export { allItems as sortedData } from './content'

export const siteURL = 'https://wweb.dev'
