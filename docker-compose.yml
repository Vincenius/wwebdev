services:
  web:
    build: .
    image: wwebdev:latest
    ports:
      - '4321:4321'
    environment:
      - TZ=UTC
      - HOST=0.0.0.0
      - PORT=4321
      # Client-exposed base hostname (was NEXT_PUBLIC_HOSTNAME).
      - PUBLIC_HOSTNAME=https://wweb.dev
    restart: unless-stopped
